<%= render  "shared/page_title", title: "Chef #{@chef.chef_name}´s profile" %>

<div class="row">
  <div class="col-md-4 offset-md-4">
    <div class="d-flex justify-content-center">
      <%= gravatar_for(@chef, size: 150) %>
    </div>
  </div>
</div>

<h4 align="center" class="mt-2"><%= "#{@chef.chef_name}´s recipes"%></h4>

<% if @chef.recipes.count > 0 %>
<div class="d-flex justify-content-center">
  <%= will_paginate @chef_recipes, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>
</div>

<%= render "recipes/recipes", obj: @chef_recipes %>
<% else %>

<h4 align="center" class="mt-2">No recipes yet!</h4>

<% end %>